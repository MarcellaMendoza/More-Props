import React from 'react'
import './Footer.css'

function Footer() {
  return(
    <footer>
      <p>Current time: (current time)</p>
      
      <p>Your next event is (event name) starting on (event time)</p>
    </footer>
  )
}

export default Footer